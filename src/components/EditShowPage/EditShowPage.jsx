import React, { Component } from "react";
import { connect } from "react-redux";

class EditShowsPages extends Component {
  render() {
    return <p>This is the Edit shows Page!</p>;
  }
}

export default connect()(EditShowsPage);
